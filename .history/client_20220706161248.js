import { createClient } from '@supabase/supabase-js'

const supabase = createClient {
    process.env.NEXT_PUBLIC_SUPABASE_URLCannot redeclare block-scoped variable 'env'.ts(2451)

}

export { supabase }